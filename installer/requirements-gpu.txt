# Elegant Transcriber - GPU Requirements
# Note: PyTorch and torchaudio are installed separately via wheel URL in setup-app.ps1
# Note: NeMo is installed normally first, then version overrides applied with --no-deps

# NVIDIA CUDA libraries
nvidia-cuda-runtime-cu12==12.8.90
nvidia-cublas-cu12==12.8.4.1
nvidia-cudnn-cu12==9.10.2.21
nvidia-ml-py

# NeMo toolkit (installs all transitive deps automatically)
nemo_toolkit[asr]>=2.7.0,<2.8.0

# Keep the resolver off numpy 2.5+: no numba wheel supports it yet, and an
# unpinned resolve backtracks to numba 0.53.1, which only ships an sdist
# whose build fails on Python 3.12 (numpy 2.5.1 + numba 0.53.1, July 2026).
numba==0.65.0
llvmlite==0.47.0
numpy<2.5

# Canary-Qwen dependencies
peft>=0.18.0
soundfile

# App-specific libraries
av
psutil
PySide6
sounddevice
pynput
nltk
fastapi
uvicorn[standard]
python-multipart
